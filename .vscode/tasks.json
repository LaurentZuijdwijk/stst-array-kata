{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pip install requirements",
            "type": "shell",
            "command": "${PYTHONPATH} -m pip install -r ${workspaceFolder}/src/requirements.txt"
        }
    ]
}